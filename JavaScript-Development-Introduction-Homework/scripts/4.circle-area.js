function calcCircleArea(r) {
	var circleArea = Math.PI * r * r;
	// alert(circleArea);
	// document.getElementById('result').innerHTML = 'area = ' + circleArea;
	document.write(circleArea);
	// document.close();
}